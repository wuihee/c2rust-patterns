# Examples

This directory stores extracted real-world snippets grouped by source project.

Suggested layout:

```text
examples/
  <project_name>/
    <pattern_name>.c
    <pattern_name>.md
```

- Keep snippets minimal but faithful to original behavior.
- Include source location and commit reference when possible.
- Redact unrelated code and keep context needed for interpretation.
